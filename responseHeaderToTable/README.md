Use this script to compare response headers from 2 URLs (by default API and APIDEV).

Run the script and add your URLs, the output will be a MD formatted table, that can be pasted into Jira, Github etc

